# 🚀 Sistema de Login e Registro em C++

## 📖 Sobre o Projeto

Este é um sistema simples de login e registro desenvolvido inteiramente em C++ (modo console). O objetivo principal é aplicar conceitos fundamentais da linguagem, com foco especial em:

* Manipulação de entrada e saída de dados (`iostream`).
* Manipulação de strings (`string`).
* Persistência de dados em arquivos (`fstream`).

Atualmente, o sistema salva os dados de usuário e senha de forma local em um arquivo `.csv`.

## ✨ Funcionalidades Atuais

* **Registro de Novos Usuários:** Permite que um novo usuário crie um login e senha.
* **Autenticação de Usuários:** Verifica se o login e a senha digitados correspondem a um registro no "banco de dados" (`.csv`).

## 💻 Tecnologias Utilizadas

* **Linguagem:** C++
* **Bibliotecas Padrão:**
    * `<iostream>` - Para interação com o usuário (console).
    * `<fstream>` - Para leitura e escrita no arquivo `.csv`.
    * `<string>` - Para manipulação de nomes de usuário e senhas.
